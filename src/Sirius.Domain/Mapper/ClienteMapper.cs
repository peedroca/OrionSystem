using System;
using System.Collections.Generic;
using System.Text;

namespace Sirius.Domain.Mapper
{
    public class ClienteMapper
    {
        /*
        public static Cliente ConvertToUserEntity(this CreateClienteModel userModel) =>
           new Cliente(0, userModel.Name, userModel.BirthDate, userModel.Cpf, userModel.Password);

        public static Cliente ConvertToUserEntity(this UpdateClienteModel userModel) =>
            new Cliente(userModel.Id, userModel.Name, userModel.BirthDate, userModel.Cpf);

        public static IEnumerable<ClienteModel> ConvertToUsers(this IList<Cliente> users) =>
            new List<ClienteModel>(users.Select(s => new WorkerModel(s.Id, s.Name.ToString(), s.BirthDate, s.Nin.ToString())));

        public static ClienteModel ConvertToUser(this Cliente user) =>
            new ClienteModel(user.Id, user.Name.ToString(), user.BirthDate, user.Nin.ToString());*/
    }
}
